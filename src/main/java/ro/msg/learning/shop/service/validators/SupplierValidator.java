package ro.msg.learning.shop.service.validators;

import ro.msg.learning.shop.domain.Supplier;

public class SupplierValidator implements Validator<Supplier> {
    @Override
    public boolean validate(Supplier itemToValidate) {
        return false;
    }
}
